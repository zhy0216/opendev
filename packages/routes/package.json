{
  "name": "@repo/routes",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@orpc/server": "latest",
    "inversify": "^7.0.0",
    "reflect-metadata": "^0.2.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@repo/auth": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/di": "workspace:*",
    "@repo/logger": "workspace:*",
    "@repo/repository": "workspace:*",
    "@repo/service": "workspace:*",
    "@repo/types": "workspace:*",
    "@repo/use-case": "workspace:*",
    "typescript": "^5.7.0"
  }
}
