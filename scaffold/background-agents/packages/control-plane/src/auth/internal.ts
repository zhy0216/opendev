/**
 * Internal API authentication for service-to-service calls.
 *
 * Re-exports from @open-inspect/shared for consistency across packages.
 */

export { generateInternalToken, verifyInternalToken } from "@open-inspect/shared";
